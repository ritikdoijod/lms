spring:
  application:
    name: auth-service

  jpa:
    hibernate:
      ddl-auto: update

  datasource:
    url: jdbc:postgresql://localhost:5432/postgres
    username: admin
    password: StrongPass

server:
  port: 4001

jwt:
  secret: ${JWT_SECRET}